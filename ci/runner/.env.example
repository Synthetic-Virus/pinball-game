# Copy to .env (gitignored) on the Docker host before `docker compose up -d --build`.
# Provide ONE of the two:
#   RUNNER_TOKEN  - short-lived registration token (re-mint when it expires)
#   ACCESS_TOKEN  - a fine-grained PAT (repo: Administration + Actions) for auto-renewal
RUNNER_TOKEN=
ACCESS_TOKEN=
